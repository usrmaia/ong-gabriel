NEXT_PUBLIC_APP_URL=http://localhost:3000 # The public URL of your Next.js application

DATABASE_URL=postgres://USER:PASSWORD@HOST:PORT/DATABASE_NAME # Prisma database connection string
DATABASE_USER=USER # docker-compose will set this automatically
DATABASE_PASSWORD=PASSWORD # docker-compose will set this automatically
DATABASE_DB=DATABASE_NAME # docker-compose will set this automatically

NEXTAUTH_URL=http://localhost:3000 # The URL of your Next.js application
AUTH_TRUST_HOST=True # [UntrustedHost]: Host must be trusted.
AUTH_SECRET=secret # Added by `npx auth secret`. Read more: https://cli.authjs.dev
AUTH_GOOGLE_ID=your-auth-google-client-id
AUTH_GOOGLE_SECRET=your-auth-google-client-secret
AUTH_FACEBOOK_ID=your-auth-facebook-client-id
AUTH_FACEBOOK_SECRET=your-auth-facebook-client-secret

LOG_FILE_ENABLED=False
SECURE_COOKIES_ENABLED=False # Set to True in production or when using HTTPS
NEXT_PUBLIC_HOTJAR_ID=1234567 # Your Hotjar ID for analytics

RATE_LIMIT_ENABLED=True
RATE_LIMIT_REQUESTS_PER_WINDOW=100
RATE_LIMIT_WINDOW_MS=60000 # 60 segundos

# Upstash Redis (Optional - for production rate limiting with persistence)
# If not provided, will use in-memory store (development only)
UPSTASH_REDIS_REST_URL=https://your-redis-url.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-redis-token
